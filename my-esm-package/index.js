/*
the `.js` extension is required!
*/
export * from './sum.js'

/*
to avoid the file extension run with `node --es-module-specifier-resolution=node`
which is maybe not quite intuitive for everyone

export * from './sum'
*/
